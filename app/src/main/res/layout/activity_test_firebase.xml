<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Firebase Integration Test"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/primary_blue"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Use these buttons to test your Firebase connection:"
            android:textSize="14sp"
            android:textColor="@color/text_gray"
            android:layout_marginBottom="24dp" />

        <!-- Load Drivers Button -->
        <Button
            android:id="@+id/btnLoadDrivers"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Load Drivers from Firebase"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/primary_blue"
            android:layout_marginBottom="12dp" />

        <!-- Seed Data Button -->
        <Button
            android:id="@+id/btnSeedData"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Add Sample Data"
            android:textColor="@color/primary_blue"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_marginBottom="12dp" />

        <!-- Clear Data Button -->
        <Button
            android:id="@+id/btnClearData"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Clear All Data (Danger)"
            android:textColor="@android:color/holo_red_dark"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_marginBottom="24dp" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/light_gray"
            android:layout_marginBottom="16dp" />

        <!-- Status Label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Status:"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginBottom="8dp" />

        <!-- Status Text -->
        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Click a button to test Firebase connection..."
            android:textSize="14sp"
            android:textColor="@color/text_gray"
            android:background="@color/light_gray"
            android:padding="16dp"
            android:minHeight="200dp"
            android:gravity="start|top" />

    </LinearLayout>
</ScrollView>